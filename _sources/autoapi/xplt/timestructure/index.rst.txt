:py:mod:`xplt.timestructure`
============================

.. py:module:: xplt.timestructure

.. autoapi-nested-parse::

   Methods for plotting particle arrival times



Module Contents
---------------

Classes
~~~~~~~

.. autoapisummary::

   xplt.timestructure.TimeBinPlot
   xplt.timestructure.TimeFFTPlot
   xplt.timestructure.TimeIntervalPlot
   xplt.timestructure.TimePlot
   xplt.timestructure.TimeVariationPlot



Functions
~~~~~~~~~

.. autoapisummary::

   xplt.timestructure.binned_timeseries



.. py:class:: TimeBinPlot(particles=None, kind='count', *, bin_time=None, bin_count=None, relative=False, mask=None, plot_kwargs=None, grid=True, ax=None, data_units=None, display_units=None, twiss=None, beta=None, frev=None, circumference=None, wrap_zeta=False, **subplots_kwargs)

   Bases: :py:obj:`xplt.particles.XParticlePlot`

   
   A binned histogram plot of particles as function of times.

   The plot is based on the particle arrival time, which is:
       - For circular lines: at_turn / frev - zeta / beta / c0
       - For linear lines: zeta / beta / c0

   The main purpose is to plot particle counts, but kind also accepts particle properties
   in which case the property is averaged over all particles falling into the bin.

   Useful to plot time structures of particles loss, such as spill structures.

   :param particles: Particles data to plot.
   :param kind: What to plot as function of time. Can be 'count' (default),
                'rate', 'cumulative', or a particle property to average.
   :type kind: str, optional
   :param bin_time: Time bin width if bin_count is None.
   :param bin_count: Number of bins if bin_time is None.
   :param relative: If True, plot relative numbers normalized to total count.
                    If what is a particle property, this has no effect.
   :param mask: An index mask to select particles to plot. If None, all particles are plotted.
   :param plot_kwargs: Keyword arguments passed to the plot function.
   :param grid: If True, show grid lines.
   :param ax: Axes to plot on. If None, a new figure is created.
   :param data_units: Units of the data. If None, the units are determined from the data.
   :type data_units: dict, optional
   :param display_units: Units to display the data in. If None, the units are determined from the data.
   :type display_units: dict, optional
   :param twiss: Twiss parameters (alfx, alfy, betx and bety) to use for conversion to normalized phase space coordinates.
   :type twiss: dict, optional
   :param beta: Relativistic beta of particles. Defaults to particles.beta0.
   :type beta: float, optional
   :param frev: Revolution frequency of circular line for calculation of particle time.
   :type frev: float, optional
   :param circumference: Path length of circular line if frev is not given.
   :type circumference: float, optional
   :param wrap_zeta: If set, wrap the zeta-coordinate plotted at the machine circumference. Either pass the circumference directly or set this to True to use the circumference from twiss.
   :param subplots_kwargs: Keyword arguments passed to matplotlib.pyplot.subplots command when a new figure is created.

   .. py:property:: axflat

      Return a flat list of all primary axes

   .. py:property:: circumference


   .. py:method:: add_scale(ax, scale, label=None, *, vertical=False, width=0.01, padding=0.1, loc='auto', color='k', fontsize='x-small')
      :staticmethod:

      Add a scale patch (a yardstick or ruler)

      :param ax: The axis to add it to.
      :param scale: The size of the scale in data units.
      :param label: A label for the scale.
      :type label: str, optional
      :param vertical: If true, make a vertical one (default is a horizontal one).
      :type vertical: bool
      :param width: The line width of the scale in axis units.
      :type width: float
      :param padding: The padding between the scale and the axis.
      :type padding: float
      :param loc: The location of the scale. Can be any of the usual matplotlib locations, e.g. 'auto', 'upper left', 'upper center', 'upper right', 'center left', 'center', 'center right', 'lower left', 'lower center, 'lower right'.
      :type loc: str
      :param color: Color for the patch.
      :param fontsize: Font size of the label.

      :returns: The artist added (an AnchoredOffsetbox).


   .. py:method:: axis_for(subplot=0, twin=0)

      Return the axis for a given flat subplot index and twin index


   .. py:method:: beta(particles=None)

      Get reference relativistic beta as float


   .. py:method:: data_unit_for(p)

      Return data unit for parameter


   .. py:method:: display_unit_for(p)

      Return display unit for parameter


   .. py:method:: factor_for(p)

      Return factor to convert parameter into display unit


   .. py:method:: frev(particles=None)

      Get reference revolution frequency


   .. py:method:: label_for(*pp, unit=True)

      Return label for list of parameters, joining where possible

      :param pp: Parameter names
      :param unit: Wheather to include unit


   .. py:method:: plot_harmonics(ax, v, dv=0, *, n=20, scale_width=True, vertical=True, inverse=False, **plot_kwargs)
      :staticmethod:

      Add vertical lines or spans indicating the location of values or spans and their harmonics

      Indicates the bands at the h-th harmonics for h = 1, 2, ..., n
      - h * (v ± dv/2)      if scale_width and not inverse (default)
      - h * v ± dv/2        if not scale_width and not inverse
      - h / (v ± dv/2)      if inverse and scale_width
      - 1 / ( v/h ± dv/2 )  if inverse and not scale_width

      :param ax: Axis to plot onto.
      :param v: Value or list of values.
      :type v: float or list of float
      :param dv: Width or list of widths centered around value(s).
      :type dv: float or list of float, optional
      :param n: Number of harmonics to plot.
      :type n: int
      :param scale_width: Whether to scale the width for higher harmonics or keep it constant.
      :type scale_width: bool, optional
      :param vertical: Plot vertical lines if true, horizontal otherweise.
      :type vertical: bool
      :param inverse: If true, plot harmonics of n/(v±dv) instead of n*(v±dv). Useful to plot frequency harmonics in time domain and vice-versa.
      :type inverse: bool
      :param plot_kwargs: Keyword arguments to be passed to plotting method


   .. py:method:: save(fname, **kwargs)

      Save the figure


   .. py:method:: set_axis_ticks_angle(yaxis, minor=True, deg=False)
      :staticmethod:

      Set ticks locator and formatter to display an angle

      This will set ticks at multiples or fractions of 180° or pi with appropriate labels

      :param yaxis: The axis to format (ax.xaxis or ax.yaxis)
      :param minor: If true (default), also set the minor locator
      :type minor: bool
      :param deg: If true, use angles in degree. If false (default), in radians.
      :type deg: bool


   .. py:method:: title(title, **kwargs)

      Set figure title


   .. py:method:: update(particles, mask=None, autoscale=False)

      Update plot with new data

      :param particles: Particles data to plot.
      :param mask: An index mask to select particles to plot. If None, all particles are plotted.
      :param autoscale: Whether or not to perform autoscaling on all axes.



.. py:class:: TimeFFTPlot(particles=None, kind='count', *, fmax=None, relative=False, log=None, scaling='amplitude', mask=None, plot_kwargs=None, grid=True, ax=None, data_units=None, display_units=None, twiss=None, beta=None, frev=None, circumference=None, wrap_zeta=False, **subplots_kwargs)

   Bases: :py:obj:`xplt.particles.XParticlePlot`

   
   A frequency plot based on particle arrival times.

   The particle arrival time is:
       - For circular lines: at_turn / frev - zeta / beta / c0
       - For linear lines: zeta / beta / c0

   From the particle arrival times (non-equally distributed timestamps), a timeseries with equally
   spaced time bins is derived. The time bin size is determined based on fmax and performance considerations.
   By default, the binned property is the number of particles arriving within the bin time (what='count').
   Alternatively, a particle property may be specified (e.g. what='x'), in which case that property is
   averaged over all particles arriving in the respective bin. The FFT is then computed over the timeseries.

   Useful to plot time structures of particles loss, such as spill structures.

   :param particles: Particles data to plot.
   :param kind: What to make the FFT over. Can be 'count' (default), or a particle property (in which case averaging applies).
   :type kind: str, optional
   :param fmax: Maximum frequency (in Hz) to plot.
   :type fmax: float
   :param relative: If True, plot relative frequencies (f/frev) instead of absolute frequencies (f).
   :type relative: bool
   :param log: If True, plot on a log scale.
   :type log: bool, optional
   :param scaling: Scaling of the FFT. Can be 'amplitude' (default) or 'pds'.
   :param mask: An index mask to select particles to plot. If None, all particles are plotted.
   :param plot_kwargs: Keyword arguments passed to the plot function.
   :param grid: If True, show grid lines.
   :param ax: Axes to plot on. If None, a new figure is created.
   :param data_units: Units of the data. If None, the units are determined from the data.
   :type data_units: dict, optional
   :param display_units: Units to display the data in. If None, the units are determined from the data.
   :type display_units: dict, optional
   :param twiss: Twiss parameters (alfx, alfy, betx and bety) to use for conversion to normalized phase space coordinates.
   :type twiss: dict, optional
   :param beta: Relativistic beta of particles. Defaults to particles.beta0.
   :type beta: float, optional
   :param frev: Revolution frequency of circular line for calculation of particle time.
   :type frev: float, optional
   :param circumference: Path length of circular line if frev is not given.
   :type circumference: float, optional
   :param wrap_zeta: If set, wrap the zeta-coordinate plotted at the machine circumference. Either pass the circumference directly or set this to True to use the circumference from twiss.
   :param subplots_kwargs: Keyword arguments passed to matplotlib.pyplot.subplots command when a new figure is created.

   .. py:property:: axflat

      Return a flat list of all primary axes

   .. py:property:: circumference


   .. py:method:: add_scale(ax, scale, label=None, *, vertical=False, width=0.01, padding=0.1, loc='auto', color='k', fontsize='x-small')
      :staticmethod:

      Add a scale patch (a yardstick or ruler)

      :param ax: The axis to add it to.
      :param scale: The size of the scale in data units.
      :param label: A label for the scale.
      :type label: str, optional
      :param vertical: If true, make a vertical one (default is a horizontal one).
      :type vertical: bool
      :param width: The line width of the scale in axis units.
      :type width: float
      :param padding: The padding between the scale and the axis.
      :type padding: float
      :param loc: The location of the scale. Can be any of the usual matplotlib locations, e.g. 'auto', 'upper left', 'upper center', 'upper right', 'center left', 'center', 'center right', 'lower left', 'lower center, 'lower right'.
      :type loc: str
      :param color: Color for the patch.
      :param fontsize: Font size of the label.

      :returns: The artist added (an AnchoredOffsetbox).


   .. py:method:: axis_for(subplot=0, twin=0)

      Return the axis for a given flat subplot index and twin index


   .. py:method:: beta(particles=None)

      Get reference relativistic beta as float


   .. py:method:: data_unit_for(p)

      Return data unit for parameter


   .. py:method:: display_unit_for(p)

      Return display unit for parameter


   .. py:method:: factor_for(p)

      Return factor to convert parameter into display unit


   .. py:method:: fmax(particles)


   .. py:method:: frev(particles=None)

      Get reference revolution frequency


   .. py:method:: label_for(*pp, unit=True)

      Return label for list of parameters, joining where possible

      :param pp: Parameter names
      :param unit: Wheather to include unit


   .. py:method:: plot_harmonics(f, df=0, *, n=20, **plot_kwargs)

      Add vertical lines or spans indicating the location of values or spans and their harmonics

      :param f: Fundamental frequency or list of frequencies in Hz.
      :type f: float or list of float
      :param df: Bandwidth or list of bandwidths centered around frequencies(s) in Hz.
      :type df: float or list of float, optional
      :param n: Number of harmonics to plot.
      :type n: int
      :param plot_kwargs: Keyword arguments to be passed to plotting method


   .. py:method:: save(fname, **kwargs)

      Save the figure


   .. py:method:: set_axis_ticks_angle(yaxis, minor=True, deg=False)
      :staticmethod:

      Set ticks locator and formatter to display an angle

      This will set ticks at multiples or fractions of 180° or pi with appropriate labels

      :param yaxis: The axis to format (ax.xaxis or ax.yaxis)
      :param minor: If true (default), also set the minor locator
      :type minor: bool
      :param deg: If true, use angles in degree. If false (default), in radians.
      :type deg: bool


   .. py:method:: title(title, **kwargs)

      Set figure title


   .. py:method:: update(particles, mask=None, autoscale=False)

      Update plot with new data

      :param particles: Particles data to plot.
      :param mask: An index mask to select particles to plot. If None, all particles are plotted.
      :param autoscale: Whether or not to perform autoscaling on all axes.



.. py:class:: TimeIntervalPlot(particles=None, *, tmax=None, bin_time=None, bin_count=None, log=True, mask=None, plot_kwargs=None, grid=True, ax=None, data_units=None, display_units=None, beta=None, frev=None, circumference=None, **subplots_kwargs)

   Bases: :py:obj:`xplt.particles.XParticlePlot`

   
   A histogram plot of particle arrival intervals (i.e. delay between consecutive particles).

   The plot is based on the particle arrival time, which is:
       - For circular lines: at_turn / frev - zeta / beta / c0
       - For linear lines: zeta / beta / c0

   Useful to plot time structures of particles loss, such as spill structures.

   :param particles: Particles data to plot.
   :param tmax: Maximum interval (in s) to plot.
   :param bin_time: Time bin width if bin_count is None.
   :param bin_count: Number of bins if bin_time is None.
   :param log: If True, plot on a log scale.
   :param mask: An index mask to select particles to plot. If None, all particles are plotted.
   :param plot_kwargs: Keyword arguments passed to the plot function.
   :param grid: If True, show grid lines.
   :param ax: Axes to plot on. If None, a new figure is created.
   :param data_units: Units of the data. If None, the units are determined from the data.
   :type data_units: dict, optional
   :param display_units: Units to display the data in. If None, the units are determined from the data.
   :type display_units: dict, optional
   :param beta: Relativistic beta of particles. Defaults to particles.beta0.
   :type beta: float, optional
   :param frev: Revolution frequency of circular line for calculation of particle time.
   :type frev: float, optional
   :param circumference: Path length of circular line if frev is not given.
   :type circumference: float, optional
   :param subplots_kwargs: Keyword arguments passed to matplotlib.pyplot.subplots command when a new figure is created.

   .. py:property:: axflat

      Return a flat list of all primary axes

   .. py:property:: bin_count


   .. py:property:: bin_time


   .. py:property:: circumference


   .. py:method:: add_scale(ax, scale, label=None, *, vertical=False, width=0.01, padding=0.1, loc='auto', color='k', fontsize='x-small')
      :staticmethod:

      Add a scale patch (a yardstick or ruler)

      :param ax: The axis to add it to.
      :param scale: The size of the scale in data units.
      :param label: A label for the scale.
      :type label: str, optional
      :param vertical: If true, make a vertical one (default is a horizontal one).
      :type vertical: bool
      :param width: The line width of the scale in axis units.
      :type width: float
      :param padding: The padding between the scale and the axis.
      :type padding: float
      :param loc: The location of the scale. Can be any of the usual matplotlib locations, e.g. 'auto', 'upper left', 'upper center', 'upper right', 'center left', 'center', 'center right', 'lower left', 'lower center, 'lower right'.
      :type loc: str
      :param color: Color for the patch.
      :param fontsize: Font size of the label.

      :returns: The artist added (an AnchoredOffsetbox).


   .. py:method:: axis_for(subplot=0, twin=0)

      Return the axis for a given flat subplot index and twin index


   .. py:method:: beta(particles=None)

      Get reference relativistic beta as float


   .. py:method:: data_unit_for(p)

      Return data unit for parameter


   .. py:method:: display_unit_for(p)

      Return display unit for parameter


   .. py:method:: factor_for(p)

      Return factor to convert parameter into display unit


   .. py:method:: frev(particles=None)

      Get reference revolution frequency


   .. py:method:: label_for(*pp, unit=True)

      Return label for list of parameters, joining where possible

      :param pp: Parameter names
      :param unit: Wheather to include unit


   .. py:method:: plot_harmonics(t, *, n=20, **plot_kwargs)

      Add vertical lines or spans indicating the location of values or spans and their harmonics

      :param t: Period in s.
      :type t: float or list of float
      :param n: Number of harmonics to plot.
      :type n: int
      :param plot_kwargs: Keyword arguments to be passed to plotting method


   .. py:method:: save(fname, **kwargs)

      Save the figure


   .. py:method:: set_axis_ticks_angle(yaxis, minor=True, deg=False)
      :staticmethod:

      Set ticks locator and formatter to display an angle

      This will set ticks at multiples or fractions of 180° or pi with appropriate labels

      :param yaxis: The axis to format (ax.xaxis or ax.yaxis)
      :param minor: If true (default), also set the minor locator
      :type minor: bool
      :param deg: If true, use angles in degree. If false (default), in radians.
      :type deg: bool


   .. py:method:: title(title, **kwargs)

      Set figure title


   .. py:method:: update(particles, mask=None, autoscale=False)

      Update plot with new data

      :param particles: Particles data to plot.
      :param mask: An index mask to select particles to plot. If None, all particles are plotted.
      :param autoscale: Whether or not to perform autoscaling on all axes.



.. py:class:: TimePlot(particles=None, kind='x+y', **kwargs)

   Bases: :py:obj:`xplt.particles.ParticlesPlot`

   
   A thin wrapper around the ParticlesPlot plotting data as function of time.
   For more information refer to the documentation of the :class:`~xplt.particles.ParticlesPlot` class.

   The plot is based on the particle arrival time, which is:
       - For circular lines: at_turn / frev - zeta / beta / c0
       - For linear lines: zeta / beta / c0


   .. py:property:: axflat

      Return a flat list of all primary axes

   .. py:property:: circumference


   .. py:method:: add_scale(ax, scale, label=None, *, vertical=False, width=0.01, padding=0.1, loc='auto', color='k', fontsize='x-small')
      :staticmethod:

      Add a scale patch (a yardstick or ruler)

      :param ax: The axis to add it to.
      :param scale: The size of the scale in data units.
      :param label: A label for the scale.
      :type label: str, optional
      :param vertical: If true, make a vertical one (default is a horizontal one).
      :type vertical: bool
      :param width: The line width of the scale in axis units.
      :type width: float
      :param padding: The padding between the scale and the axis.
      :type padding: float
      :param loc: The location of the scale. Can be any of the usual matplotlib locations, e.g. 'auto', 'upper left', 'upper center', 'upper right', 'center left', 'center', 'center right', 'lower left', 'lower center, 'lower right'.
      :type loc: str
      :param color: Color for the patch.
      :param fontsize: Font size of the label.

      :returns: The artist added (an AnchoredOffsetbox).


   .. py:method:: axis_for(subplot=0, twin=0)

      Return the axis for a given flat subplot index and twin index


   .. py:method:: beta(particles=None)

      Get reference relativistic beta as float


   .. py:method:: data_unit_for(p)

      Return data unit for parameter


   .. py:method:: display_unit_for(p)

      Return display unit for parameter


   .. py:method:: factor_for(p)

      Return factor to convert parameter into display unit


   .. py:method:: frev(particles=None)

      Get reference revolution frequency


   .. py:method:: label_for(*pp, unit=True)

      Return label for list of parameters, joining where possible

      :param pp: Parameter names
      :param unit: Wheather to include unit


   .. py:method:: plot_harmonics(ax, v, dv=0, *, n=20, scale_width=True, vertical=True, inverse=False, **plot_kwargs)
      :staticmethod:

      Add vertical lines or spans indicating the location of values or spans and their harmonics

      Indicates the bands at the h-th harmonics for h = 1, 2, ..., n
      - h * (v ± dv/2)      if scale_width and not inverse (default)
      - h * v ± dv/2        if not scale_width and not inverse
      - h / (v ± dv/2)      if inverse and scale_width
      - 1 / ( v/h ± dv/2 )  if inverse and not scale_width

      :param ax: Axis to plot onto.
      :param v: Value or list of values.
      :type v: float or list of float
      :param dv: Width or list of widths centered around value(s).
      :type dv: float or list of float, optional
      :param n: Number of harmonics to plot.
      :type n: int
      :param scale_width: Whether to scale the width for higher harmonics or keep it constant.
      :type scale_width: bool, optional
      :param vertical: Plot vertical lines if true, horizontal otherweise.
      :type vertical: bool
      :param inverse: If true, plot harmonics of n/(v±dv) instead of n*(v±dv). Useful to plot frequency harmonics in time domain and vice-versa.
      :type inverse: bool
      :param plot_kwargs: Keyword arguments to be passed to plotting method


   .. py:method:: save(fname, **kwargs)

      Save the figure


   .. py:method:: set_axis_ticks_angle(yaxis, minor=True, deg=False)
      :staticmethod:

      Set ticks locator and formatter to display an angle

      This will set ticks at multiples or fractions of 180° or pi with appropriate labels

      :param yaxis: The axis to format (ax.xaxis or ax.yaxis)
      :param minor: If true (default), also set the minor locator
      :type minor: bool
      :param deg: If true, use angles in degree. If false (default), in radians.
      :type deg: bool


   .. py:method:: title(title, **kwargs)

      Set figure title


   .. py:method:: update(particles, mask=None, autoscale=False)

      Update plot with new data

      :param particles: Particles data to plot.
      :param mask: An index mask to select particles to plot. If None, all particles are plotted.
      :param autoscale: Whether or not to perform autoscaling on all axes.



.. py:class:: TimeVariationPlot(particles=None, metric='cv', *, counting_dt=None, counting_bins=None, evaluate_dt=None, evaluate_bins=None, poisson=True, mask=None, plot_kwargs=None, grid=True, ax=None, data_units=None, display_units=None, beta=None, frev=None, circumference=None, **subplots_kwargs)

   Bases: :py:obj:`xplt.particles.XParticlePlot`

   
   Plot for variability of particles arriving as function of arrival time

   The plot is based on the particle arrival time, which is:
       - For circular lines: at_turn / frev - zeta / beta / c0
       - For linear lines: zeta / beta / c0

   Useful to plot time structures of particles loss, such as spill structures.

   The following metrics are implemented:
       cv: Coefficient of variation
           cv = std(N)/mean(N)
       duty: Spill duty factor
           F = mean(N)**2 / mean(N**2)

   :param particles: Particles data to plot.
   :param metric: Metric to plot. See above for list of implemented metrics.
   :type metric: str
   :param counting_dt: Time bin width for counting if counting_bins is None.
   :param counting_bins: Number of bins if counting_dt is None.
   :param evaluate_dt: Time bin width for metric evaluation if evaluate_bins is None.
   :param evaluate_bins: Number of bins if evaluate_dt is None.
   :param poisson: If true, indicate poisson limit.
   :type poisson: bool
   :param mask: An index mask to select particles to plot. If None, all particles are plotted.
   :param plot_kwargs: Keyword arguments passed to the plot function.
   :param grid: If True, show grid lines.
   :param ax: Axes to plot on. If None, a new figure is created.
   :param data_units: Units of the data. If None, the units are determined from the data.
   :type data_units: dict, optional
   :param display_units: Units to display the data in. If None, the units are determined from the data.
   :type display_units: dict, optional
   :param beta: Relativistic beta of particles. Defaults to particles.beta0.
   :type beta: float, optional
   :param frev: Revolution frequency of circular line for calculation of particle time.
   :type frev: float, optional
   :param circumference: Path length of circular line if frev is not given.
   :type circumference: float, optional
   :param subplots_kwargs: Keyword arguments passed to matplotlib.pyplot.subplots command when a new figure is created.

   .. py:property:: axflat

      Return a flat list of all primary axes

   .. py:property:: circumference


   .. py:method:: add_scale(ax, scale, label=None, *, vertical=False, width=0.01, padding=0.1, loc='auto', color='k', fontsize='x-small')
      :staticmethod:

      Add a scale patch (a yardstick or ruler)

      :param ax: The axis to add it to.
      :param scale: The size of the scale in data units.
      :param label: A label for the scale.
      :type label: str, optional
      :param vertical: If true, make a vertical one (default is a horizontal one).
      :type vertical: bool
      :param width: The line width of the scale in axis units.
      :type width: float
      :param padding: The padding between the scale and the axis.
      :type padding: float
      :param loc: The location of the scale. Can be any of the usual matplotlib locations, e.g. 'auto', 'upper left', 'upper center', 'upper right', 'center left', 'center', 'center right', 'lower left', 'lower center, 'lower right'.
      :type loc: str
      :param color: Color for the patch.
      :param fontsize: Font size of the label.

      :returns: The artist added (an AnchoredOffsetbox).


   .. py:method:: axis_for(subplot=0, twin=0)

      Return the axis for a given flat subplot index and twin index


   .. py:method:: beta(particles=None)

      Get reference relativistic beta as float


   .. py:method:: data_unit_for(p)

      Return data unit for parameter


   .. py:method:: display_unit_for(p)

      Return display unit for parameter


   .. py:method:: factor_for(p)

      Return factor to convert parameter into display unit


   .. py:method:: frev(particles=None)

      Get reference revolution frequency


   .. py:method:: label_for(*pp, unit=True)

      Return label for list of parameters, joining where possible

      :param pp: Parameter names
      :param unit: Wheather to include unit


   .. py:method:: plot_harmonics(ax, v, dv=0, *, n=20, scale_width=True, vertical=True, inverse=False, **plot_kwargs)
      :staticmethod:

      Add vertical lines or spans indicating the location of values or spans and their harmonics

      Indicates the bands at the h-th harmonics for h = 1, 2, ..., n
      - h * (v ± dv/2)      if scale_width and not inverse (default)
      - h * v ± dv/2        if not scale_width and not inverse
      - h / (v ± dv/2)      if inverse and scale_width
      - 1 / ( v/h ± dv/2 )  if inverse and not scale_width

      :param ax: Axis to plot onto.
      :param v: Value or list of values.
      :type v: float or list of float
      :param dv: Width or list of widths centered around value(s).
      :type dv: float or list of float, optional
      :param n: Number of harmonics to plot.
      :type n: int
      :param scale_width: Whether to scale the width for higher harmonics or keep it constant.
      :type scale_width: bool, optional
      :param vertical: Plot vertical lines if true, horizontal otherweise.
      :type vertical: bool
      :param inverse: If true, plot harmonics of n/(v±dv) instead of n*(v±dv). Useful to plot frequency harmonics in time domain and vice-versa.
      :type inverse: bool
      :param plot_kwargs: Keyword arguments to be passed to plotting method


   .. py:method:: save(fname, **kwargs)

      Save the figure


   .. py:method:: set_axis_ticks_angle(yaxis, minor=True, deg=False)
      :staticmethod:

      Set ticks locator and formatter to display an angle

      This will set ticks at multiples or fractions of 180° or pi with appropriate labels

      :param yaxis: The axis to format (ax.xaxis or ax.yaxis)
      :param minor: If true (default), also set the minor locator
      :type minor: bool
      :param deg: If true, use angles in degree. If false (default), in radians.
      :type deg: bool


   .. py:method:: title(title, **kwargs)

      Set figure title


   .. py:method:: update(particles, mask=None, autoscale=False)

      Update plot with new data

      :param particles: Particles data to plot.
      :param mask: An index mask to select particles to plot. If None, all particles are plotted.
      :param autoscale: Whether or not to perform autoscaling on all axes.



.. py:function:: binned_timeseries(times, n, what=None)

   Get binned timeseries with equally spaced time bins

   From the particle arrival times (non-equally distributed timestamps), a timeseries with equally
   spaced time bins is derived. The time bin size is determined based on the number of bins.
   The parameter `what` determines what is returned for the timeseries. By default (what=None), the
   number of particles arriving within each time bin is returned. Alternatively, a particle property
   can be passed as array, in which case that property is averaged over all particles arriving within
   the respective bin (or 0 if no particles arrive within a time bin).

   :param times: Array of particle arrival times.
   :param what: Array of associated data or None. Must have same shape as times. See above.
   :param n: Number of bins.
   :param range: Tuple of (min, max) time values to consider. If None, the range is determined from the data.

   :returns: The timeseries as tuple (t_min, dt, values) where
             t_min is the start time of the timeseries data,
             dt is the time bin width and
             values are the values of the timeseries as array of length n.


